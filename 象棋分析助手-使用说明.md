# 天天象棋本机分析助手

现在由中国象棋专用引擎 [Pikafish](https://github.com/official-pikafish/Pikafish) 和它的 NNUE 网络负责分析。Python 只负责连接引擎与浏览器；浏览器扩展读取棋盘、发送 FEN，并标出推荐走法。搜索不会阻塞游戏页面的主线程。

## 启动

1. 从 [Releases](https://github.com/3DMXM/xiangqi-advisor/releases) 下载并解压 Windows 完整包。先关闭旧版分析服务窗口，再双击 `start-xiangqi-advisor.cmd`，保持新窗口运行。也可以在本目录执行 `py -3 xiangqi_advisor_server.py`。
2. 在 Chrome 或 Edge 的扩展管理页开启“开发者模式”，点击“加载已解压的扩展程序”，选择项目中的 `extension` 文件夹，然后刷新天天象棋页面。停用旧版 Tampermonkey 脚本，避免出现两个面板。
3. 进入人机或真人对战棋盘，在面板中设置“我执”红方或黑方。“对局模式”默认自动识别；若无法找到人机棋盘，手动选择“人机对局”，再点击“识别当前棋盘”。默认自动跟踪棋盘；轮到我方时自动分析。手动点击“分析”仍可分析任意已载入的局面。
4. 如果希望扩展代为下棋，勾选“自动落子”。它默认关闭；启用后仅对刚刚识别的实战局面执行 Pikafish 的最佳走法。改动过的棋盘或手动输入的 FEN 只会显示推荐，不会触发点击。

自动落子会在点击前再次核对棋盘、行棋方和推荐着法，并通过游戏画布点击起点与终点。若游戏没有接受点击，扩展会取消勾选“自动落子”并显示错误；此时请手动落子，或确认棋盘状态后重新开启。切换执棋方时请同步修改“我执”。

真人对战通过游戏显示的双方计时状态判断轮到谁，并从棋盘控制器读取你执的棋色；“我执”设置不一致时面板会提示调整。对局尚未开始、暂停、结束或无法确定行棋方时，不会自动分析或落子。棋盘格距优先读取游戏组件；人机棋盘仍兼容旧版格距。

分析面板按游戏画面中的棋盘朝向显示红黑双方。若游戏翻转棋盘，面板会在下一次自动识别后同步旋转；推荐箭头和自动落子的起止格仍以同一局面坐标计算。

服务只监听本机 `127.0.0.1:8765`，不需要安装第三方 Python 包。面板可选 2、5、10 秒思考时间；时间越长，通常能搜索得越深入，但不保证每步都优于较短分析。

引擎文件位于 `third_party/pikafish`，来自官方 [Pikafish 2026-09-06 发行版](https://github.com/official-pikafish/Pikafish/releases/tag/Pikafish-2026-09-06)。该目录保留了引擎和 NNUE 网络的许可文件。分析使用 UCI 协议，默认 2 个搜索线程、64 MB 哈希缓存，只返回一条最佳走法。

若面板显示“分析服务未连接”，确认命令行窗口仍在运行，然后等待自动重试或点击“分析”。关闭命令行窗口会停止本机服务。
